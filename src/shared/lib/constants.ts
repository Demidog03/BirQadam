import { generate } from '@ant-design/colors';

const PRIMARY = generate('#40679E');
const SECONDARY = generate('#8AACE8');
const ERROR = generate('#69b938');
const LIGHT = generate('#F7FAFC');

export const COLORS = {
  PRIMARY,
  SECONDARY,
  ERROR,
  LIGHT
}